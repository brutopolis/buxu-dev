:(hello world!) :str rename @
register @@
str print @

# (we expect 451 to be printed
    because if the result of a list is only one item, 
    it is returned as a single value)

(400 51 add @) print @

# (we expect 7 6 to be printed)
(1 2 3 4 5 6 7) :numbers rename @
register @@

2 numbers lpop @ print @ print @

